export function activate(): void {
  // VS Code activation wiring will be added once the WebSocket client and
  // workspace provider are connected to the real protocol.
}

export function deactivate(): void {}
